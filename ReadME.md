# Real-time Sensor Data Display

A Flask application that displays real-time sensor data on a local web page.

## Getting Started

1. Install Python 3.x and Flask (`pip install flask`).
2. Clone the repository: `git clone https://github.com/your-username/real-time-sensor-data-display.git`
3. Install dependencies: `pip install -r requirements.txt`
4. Run: `python sensor_output.py`
5. Open browser: `http://localhost:8080/`

## Usage

- The application fetches real-time sensor data and displays it in a grid format.
- Cards change color based on object status (Active: green, Inactive: red).
- Users can select multiple cards by clicking (yellow border indicates selection).

## License

MIT License. See [LICENSE](LICENSE) for details.
